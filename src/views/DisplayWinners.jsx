import React from "react";

import { Layout } from "../layout";
import { Winners } from "../components/Championships";

const DisplayWinners = () => {
  return (
    <Layout>
      <Winners />
    </Layout>
  );
};

export default DisplayWinners;
